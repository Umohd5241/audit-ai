{
  "entities": {
    "User": {
      "title": "User",
      "description": "A registered user of the application.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "email": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "email", "phoneNumber", "createdAt"]
    },
    "IdeaRoom": {
      "title": "Idea Room",
      "description": "Represents a startup idea validation space.",
      "type": "object",
      "properties": {
        "roomId": { "type": "string" },
        "userId": { "type": "string" },
        "ideaName": { "type": "string" },
        "description": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["roomId", "userId", "ideaName", "description", "createdAt"]
    },
    "Message": {
      "title": "Message",
      "description": "A message within an idea room conversation.",
      "type": "object",
      "properties": {
        "messageId": { "type": "string" },
        "roomId": { "type": "string" },
        "userId": { "type": "string" },
        "sender": { "type": "string", "enum": ["user", "ai"] },
        "content": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["messageId", "roomId", "userId", "sender", "content", "timestamp"]
    },
    "WhatsAppSession": {
      "title": "WhatsApp Session",
      "description": "Maps a WhatsApp phone number to an active Idea Room.",
      "type": "object",
      "properties": {
        "phoneNumber": { "type": "string" },
        "currentRoomId": { "type": "string" },
        "userId": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["phoneNumber", "currentRoomId", "updatedAt"]
    },
    "Report": {
      "title": "Report",
      "description": "A startup evaluation report generated by AI.",
      "type": "object",
      "properties": {
        "reportId": { "type": "string" },
        "roomId": { "type": "string" },
        "userId": { "type": "string" },
        "score": { "type": "integer" },
        "summary": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["reportId", "roomId", "userId", "score", "summary", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Collection of user profiles."
    },
    "/ideaRooms/{roomId}": {
      "schema": { "$ref": "#/entities/IdeaRoom" },
      "description": "Collection of startup idea rooms."
    },
    "/ideaRooms/{roomId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/Message" },
      "description": "Subcollection of messages within a room."
    },
    "/whatsappSessions/{phoneNumber}": {
      "schema": { "$ref": "#/entities/WhatsAppSession" },
      "description": "Internal mapping of phone numbers to rooms."
    },
    "/reports/{reportId}": {
      "schema": { "$ref": "#/entities/Report" },
      "description": "Collection of generated evaluation reports."
    }
  }
}
